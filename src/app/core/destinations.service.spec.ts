import { DestinationsService } from './destinations.service';

describe('DestionationsService', () => {
  let service: DestinationsService;

  beforeEach(() => {
    service = new DestinationsService();
  });


});
